<header>
  <nav class="navbar">
    </a>C&H</a>

    </div>
    <main style="margin: 1em;">
      <% if (user) { %>

      <form class="form-inline" action="/search/" method="POST">
        <input type="text" id="search" name="search">
        <button type='submit'>SEARCH</button>
      </form>

      <form class="form-inline" action="/users/profile/<%= user %>" method="GET">
        <button type="submit" value="submit">EDIT PROFILE</button>
      </form>

      <form class="form-inline" action="/resources/new" method="GET">
        <button type="submit" value="submit">ADD NEW</button>
      </form>

      <h3>Logged in as:</h3>
      <h3><%= user %></h3>
      <form class="form-inline" action="/logout" method="POST">
        <button type="submit" value="submit">Log Out</button>
      </form>

      <form class="form-inline" action="/users/login/<%= user %>" method="GET">
        <button type="submit" value="submit">MY RESOURCES</button>
      </form>

      <%} else {%>
      <form class="form-inline" action="/login" method="POST">
        <button><a href="/login">Login</a></button>
      </form>
      <a href="register" class="btn btn-primary">Register</a>
      <% } %>

    </main>
  </nav>

</header>
